import React from 'react';
import TransactionForm from './TransactionForm';

const AddTransactionScreen = (props) => <TransactionForm {...props} type="income" />;

export default AddTransactionScreen;
